<div class="container pb-3 <%= cycle "bg-light", "bg-white" %>" id="skills">
  <div class="row pt-3">
    <% @person.items.skills.each do |skill| %>
      <%= render "items/skill", skill: skill %>
    <% end %>
  </div>
</div>
